# Copier ce fichier en .env (jamais commité) et renseigner votre propre clé.
MISTRAL_API_KEY=

# Clé de VOTRE choix (pas fournie par Mistral) protégeant POST /rebuild — à envoyer dans
# l'en-tête HTTP "X-API-Key" pour déclencher une reconstruction de la base vectorielle.
X_API_KEY=
